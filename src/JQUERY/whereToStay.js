$(document).ready(function(){    $(document).on('submit','#modalToWhereToStay',function(event){        event.preventDefault()        const DataForm = new  FormData(this)        DataForm.append('action','store')        $.ajax({            url: '../controllers/where_to_stayController.php',            type: 'POST',            data: DataForm,            dataType: 'json',            contentType: false,            processData: false,            success: function (res){                console.log(res)                if (res.success === true){                    Swal.fire({                        text: res.message,                        icon: "success"                    });                    $('#modalToWhereToStay')[0].reset()                    // setTimeout(() =>{window.location.reload()},1000)                }                if (res.success === false){                    Swal.fire({                        text: res.message,                        icon: "error"                    });                }            }        })    })})