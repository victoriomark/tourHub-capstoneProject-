$(document).ready(function (){   // for adding event    $(document).on('submit','#modalCreateTourist',function (event){        event.preventDefault()        const data = new FormData(this)        data.append('action','store')        // handle ajax request        $.ajax({          url: '../controllers/touristController.php',          type: 'post',          data: data,          contentType: false,          processData: false,          dataType: 'json',          success: function (res){              function MessageEroor(fild,message) {                  if (message) {                      $(`#${fild}_msg`).html(message)                      $(`#${fild}`).addClass('is-invalid')                  } else {                      $(`#${fild}_msg`).html('')                      $(`#${fild}`).removeClass('is-invalid')                  }              }           if (res.errors){               MessageEroor('Title', res.errors.Title)               MessageEroor('Description', res.errors.Description)               MessageEroor('Location', res.errors.Location)                MessageEroor('municipality_Id', res.errors.municipality_Id)               return;           }              if (res.success === true){                  Swal.fire({                      text: res.message,                      icon: "success"                  });                  setTimeout(() =>{window.location.reload()},1000)              }              if (res.success === false){                  Swal.fire({                      text: res.message,                      icon: "error"                  });              }          }        })    })    // functionality for viewing images base on tourist ID    $(document).on('click','#btn_view_gallary',function (Id){        // ajax Requesrt for sending the Id into Tourist Controller        $.ajax({            url: '../controllers/touristController.php',            type: 'post',            data: {TouristId: Id.target.value,action: 'getGallery'},            success: function (data){                $('body').append(data);                $(`#ShowGalleryModal_${Id.target.value}`).modal('show')            }        })    })    // show by id    $(document).on('click','#btn_edit',function (Id){        // ajax Requesrt for sending the Id into Tourist Controller        $.ajax({            url: '../controllers/touristController.php',            type: 'post',            data: {Id: Id.target.value,action: 'showById'},            success: function (data){                $('body').append(data);                $(`#UpdateModal_${Id.target.value}`).modal('show')            }        })    })    $(document).on('submit','.updateTouristModal',function (event){        event.preventDefault()        var formData = new FormData(this)        formData.append('action', 'update')        // ajax Request        $.ajax({            url: '../controllers/touristController.php',            type: 'post',            processData: false,            contentType: false,            data: formData,            dataType: 'json',            success: function (res){                if (res.success === true){                    Swal.fire({                        text: res.message,                        icon: "success"                    });                    setTimeout(() =>{window.location.reload()},1000)                }                if (res.success === false){                    Swal.fire({                        text: res.message,                        icon: "error"                    });                }            }        })    })    $(document).on('click','#btn_delete',function (Id){        Swal.fire({            title: "Are you sure?",            text: "You won't be able to revert this!",            icon: "warning",            showCancelButton: true,            confirmButtonColor: "#3085d6",            cancelButtonColor: "#d33",            confirmButtonText: "Yes, delete it!"        }).then((result) => {            if (result.isConfirmed) {               $.ajax({                   url: '../controllers/touristController.php',                   type: 'post',                   data:{Id: Id.target.value, action: 'delete'},                   dataType: 'json',                   success: function (res){                       if (res.success === true){                           Swal.fire({                               text: res.message,                               icon: "success"                           });                           setTimeout(() =>{window.location.reload()},1000)                       }                       if (res.success === false){                           Swal.fire({                               text: res.message,                               icon: "error"                           });                       }                   }               })            }        });    })})